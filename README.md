# accumulation-amount
Script for calculating the daily percentage of cumulative revenue achievement against cumulative targets (forecasts) using UNION ALL and window functions.

Technologies
SQL, Google BigQuery, Window Functions (SUM OVER), Data Blending, Cumulative Aggregation
